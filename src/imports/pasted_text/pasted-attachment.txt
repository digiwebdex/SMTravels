[CONTINUATION HEADER]

CONTINUE THE SAME PROJECT — SM Travels International.
This is the FINAL BRAND ALIGNMENT PASS on the complete design we have already
built together (Design System, Public Website, Auth, all ERP modules, all
Portals, Responsive Tablet/Mobile versions, Sitemap & Prototype).

Do NOT redesign layouts. Do NOT rebuild screens. Do NOT change any
functionality — this is a LIVE project. Keep every existing screen, component,
field, table, form, flow, and prototype link exactly as it is.

The ONLY change: replace the current old palette with the OFFICIAL COMPANY
BRAND COLORS taken from the SM Travels International logo, and apply them
consistently across 100% of the project.

=== OFFICIAL BRAND PALETTE (from the logo) ===
The logo is an orange airplane with a blue flight-path swoosh, the word "SM" in
orange, "Travels" in bright blue, and "International" in deep navy blue.

Primary — Brand Blue:      #0E6BB8   (hover #0B5794, active #094A7E)
Deep Navy (headings/nav):  #17456B   (the "International" text + swoosh navy)
Accent — Brand Orange:     #E8471F   (hover #CC3C17, active #B23413)
Orange text on white:      #C43A15   (darkened for WCAG AA on white bg)
Light Blue tint:           #E8F2FA   (selected rows, active nav bg, info bg)
Light Orange tint:         #FDEDE8   (accent highlights, alert backgrounds)

Neutrals (keep exactly as-is — already correct in theme.css):
Background #F7F8FA · Surface #FFFFFF · Border #E5E7EB
Text primary #111827 · secondary #6B7280 · muted #9CA3AF

Semantic (keep meaning, harmonize hue with brand):
Success #16A34A · Warning #F59E0B · Danger #DC2626 · Info = Brand Blue #0E6BB8

=== HOW TO APPLY THE COLORS ===
- Primary buttons, active nav items, links, focus rings, selected states,
  progress bars, primary chart series → Brand Blue #0E6BB8.
- Sidebar background, top bar, ERP shell, footer, table headers, page
  headings, dark surfaces → Deep Navy #17456B.
- Orange #E8471F is the ACCENT ONLY — use it sparingly and deliberately for:
  the main hero CTA ("Book Now" / "Get Quote"), key call-to-action buttons on
  the public website, important badges/counts, notification dots, the active
  step in wizards, and secondary chart series. Do NOT use orange as a large
  background fill or for body text. Target roughly 70% blue / 20% neutral /
  10% orange across any given screen.
- For any orange text rendered on a white/light background, use #C43A15
  (the darkened variant) to pass WCAG AA contrast — never #E8471F on white text.
- Charts: Brand Blue → Orange → Deep Navy → Light Blue → Success Green, in
  that order.
- Status badges keep their semantic meaning (Pending, Confirmed, Processing,
  Completed, Cancelled, Paid, Partial, Overdue) — only retune so they sit
  comfortably beside the new palette. "Info"-type badges → Light Blue tint.
- Gradients (hero, KPI cards, login split-screen): Brand Blue → Deep Navy,
  with an optional subtle orange accent element. No gold anywhere.
- ERP background stays #F7F8FA (already neutral — no change needed).

=== REMOVE COMPLETELY ===
Delete every trace of the old palette from tokens, components, illustrations,
icons, borders, shadows, gradients, and chart colors:

OLD VALUES TO REPLACE (exhaustive list — grep these exact strings):
  #14356B  →  context-dependent: sidebar/nav/dark surfaces → #17456B;
              buttons/links/primary actions → #0E6BB8
  #0f2a56  →  #0B5794  (primary button hover)
  #0F2A55  →  #0B5794  (same, different case — both must go)
  #C9A227  →  #E8471F  (accent / CTA / badges)
  #b8911f  →  #CC3C17  (gold hover → orange hover)
  #C9A22740  →  #E8471F40  (translucent gold borders)
  #0E7C66  →  #16A34A  (old emerald accent → standard success green)
              Exception: keep #0E7C66 where it is used as "Accountant Portal"
              brand accent — change those instances to #0E6BB8 instead.
  bg-\[#14356B\]  →  bg-\[#17456B\] (sidebar) or bg-\[#0E6BB8\] (buttons)
  text-\[#14356B\]  →  text-\[#0E6BB8\]
  border-\[#14356B\]  →  border-\[#0E6BB8\]
  ring-\[#14356B\]  →  ring-\[#0E6BB8\]
  fill-\[#14356B\]  →  fill-\[#17456B\]

Nothing gold (#C9A227 or any amber used as a brand color, not a semantic
warning) should remain anywhere in the file.

=== START HERE — UPDATE TOKENS FIRST (cascades to everything) ===
The token layer is the fastest path. Update these two files before touching
any component, and the change will cascade through most of the codebase
automatically:

1. src/styles/theme.css  — CSS custom properties used by Tailwind:
   --primary:           #14356B  →  #0E6BB8
   --accent:            #C9A227  →  #E8471F
   --ring:              #14356B  →  #0E6BB8
   --chart-1:           #14356B  →  #0E6BB8
   --chart-2:           #C9A227  →  #E8471F
   --chart-3:           #0E7C66  →  #16A34A
   --chart-4:           #2563EB  →  #E8F2FA  (light blue as 4th series)
   --sidebar:           #14356B  →  #17456B
   --sidebar-primary:   #C9A227  →  #E8471F
   --sidebar-primary-foreground: #14356B  →  #FFFFFF
   --sidebar-ring:      #C9A227  →  #0E6BB8

2. src/app/lib/ds.tsx  — BRAND constants (canonical source of truth):
   BRAND.navy    = "#14356B"  →  "#0E6BB8"   (primary blue)
   BRAND.navyDk  = "#0f2a56"  →  "#0B5794"   (primary blue hover)
   BRAND.gold    = "#C9A227"  →  "#E8471F"   (accent orange)
   BRAND.emerald = "#0E7C66"  →  "#16A34A"   (success green — semantic only)
   BRAND.erpBg   = "#F0F2F5"  →  "#F7F8FA"   (align with theme.css --background)
   Add new constant:
   BRAND.deepNavy = "#17456B"                (sidebar / dark surfaces)
   Add new constant:
   BRAND.orangeText = "#C43A15"              (orange on white, WCAG AA safe)

After updating these two files, sweep the remaining inline hex values in all
component and page files as listed in the "REMOVE COMPLETELY" section above.

=== LOGO & IDENTITY ===
Build an inline SVG logo component (src/app/components/Logo.tsx) that renders
the SM Travels International mark in code — an orange airplane silhouette (✈)
with a blue arc/swoosh beneath it, "SM" in Brand Orange (#E8471F), "Travels"
in Brand Blue (#0E6BB8), "International" in Deep Navy (#17456B).

Provide these variants as props:
  variant="full"      — full wordmark (plane + swoosh + all three text parts)
  variant="mark"      — plane + swoosh only (no text, for favicon/collapsed sidebar)
  variant="white"     — full wordmark in all-white (for dark navy/dark backgrounds)
  theme="light"       — full color on white/light surface (default)
  theme="dark"        — white reversed version for navy sidebar and dark surfaces

Place the logo in:
  • Public website header (full, light) and footer (white on navy)
  • ERP sidebar top — full when expanded, mark-only when collapsed
  • All portal sidebars top — full variant
  • Login / Auth split-screen brand panel — white variant on navy background
  • Invoice and receipt header (full, light)
  • The /ds Design System page logo showcase section
  • Mobile top bar — mark-only (compact, saves space)

Maintain clear space equal to the height of the airplane icon on all sides.
Never stretch, recolour, skew, or add drop shadows to the logo.

=== CONSISTENCY SWEEP (update every area) ===
After updating the token files and Logo component, sweep every file below.
In each case: replace old hex values, update any hardcoded style={{ color }}
or style={{ backgroundColor }} inline styles, update Tailwind class strings:

Design System (/ds)
  → Update token swatches on the page to show new values
  → Re-label "Navy #14356B" → "Brand Blue #0E6BB8" and "Gold #C9A227" →
    "Brand Orange #E8471F" and add "Deep Navy #17456B"
  → Add logo section showing all four variants

Public Website
  Home, About, Hajj, Umrah, Visa, Air Ticket, Manpower, Tour Packages, Hotel,
  Packages list/detail, Blog, Gallery, FAQ, Contact, Booking Request (/book)
  → Hero CTAs: primary button → Brand Blue; "Book Now" CTA → Brand Orange
  → Nav active states → Brand Blue underline
  → Footer background → Deep Navy

Auth (/login, /register)
  → Split-screen brand panel background → Deep Navy #17456B
  → Primary buttons → Brand Blue
  → Focus rings → Brand Blue
  → Logo → white variant on the navy panel

ERP Shell (src/app/erp/ErpLayout.tsx)
  → Sidebar background → Deep Navy #17456B
  → Active nav item highlight → Brand Blue #0E6BB8 tint (white/15%)
  → Topbar border accent → Brand Blue
  → Mobile bottom nav active → Brand Blue

ERP Modules (all files in src/app/erp/):
  SuperAdminDashboard, BookingsModule, PackageManagement, ServicesConfig,
  AccountsModule, InvoicesModule, ReportsModule, ReportsBIModule,
  DocumentsModule, CommunicationsModule, CmsModule, OperationsModule,
  SettingsModule
  → Replace all #14356B → #0E6BB8 (buttons, links, active states)
  → Replace all #C9A227 → #E8471F (accent badges, highlights)
  → Chart colors → follow new chart order (Blue → Orange → Navy → etc.)

Customer Portal (src/app/portal/CustomerPortal.tsx) — route: /portal
Agent Portal    (src/app/portal/AgentPortal.tsx)    — route: /agent
Supplier Portal (src/app/portal/SupplierPortal.tsx) — route: /supplier
Staff Portal    (src/app/portal/StaffPortal.tsx)    — route: /staff
Accountant Portal (src/app/portal/AccountantPortal.tsx) — route: /accountant
  → Each portal sidebar → Deep Navy #17456B
  → Active states → Brand Blue #0E6BB8
  → CTAs / wallet hero / commission tier badge → Brand Blue or Orange as
    contextually appropriate
  → Remove all gold references

Responsive frames (mobile/tablet) — already handled by the portal files above;
no separate frame files exist. The responsive layout uses Tailwind breakpoints
inline in the same component files.

Sitemap & Workflow (/sitemap — src/app/pages/Sitemap.tsx)
  → Zone panel border/badge colors: update the ZONE_CFG object
    - "erp" zone: was navy → now Deep Navy #17456B
    - Primary flow step cards: was #14356B → #0E6BB8 for buttons/Brand Blue nodes
    - Entry point / payment flow nodes → Brand Orange #E8471F
    - Connector lines/arrows → Deep Navy #17456B
  → Portal connection grid → update per-zone dot colors

Design System showcase (/ds — src/app/pages/DesignSystem.tsx)
  → Color swatch section → update hex values and labels
  → Button/badge demos → will update automatically from ds.tsx BRAND constants
  → Add "Brand Style Guide" sub-section (see DELIVER below)

=== ACCESSIBILITY CHECK ===
After replacing colors, verify:
  ✓ White text on Brand Blue #0E6BB8 — passes WCAG AA (contrast ~4.6:1) ✓
  ✓ White text on Deep Navy #17456B — passes WCAG AA (contrast ~7.2:1) ✓
  ✗ Orange #E8471F on white text — FAILS WCAG AA — use #C43A15 instead for
    any text rendered on white or light backgrounds
  ✓ Focus rings: change ring color from #14356B to #0E6BB8; ensure 3px ring
    is visible against both white and light-blue backgrounds
  ✓ Bangla text (Noto Sans Bengali) and English text must remain legible on
    Deep Navy sidebar — use white (#FFFFFF) not grey for sidebar text
  ✓ Status badge semantic colors: confirm all badge text/bg combos pass AA

=== DELIVER ===
1. Updated src/styles/theme.css with new token values (ready for copy-paste
   into a Tailwind config).

2. Updated src/app/lib/ds.tsx with new BRAND constants and the Logo component
   moved to src/app/components/Logo.tsx.

3. A "Brand Style Guide" section added to the /ds Design System page showing:
   - Logo variants (full / mark / white) rendered as inline SVG
   - Full colour palette with hex codes, usage rules, and DO / DON'T examples
   - Typography scale (unchanged — Inter + Noto Sans Bengali)
   - Button, badge, and chart colour examples using new palette
   - WCAG contrast ratios for key pairings
   This section should be self-contained and printable as a one-pager.

4. A short written summary listing:
   - Every file that was changed
   - The final token values (suitable for pasting into tailwind.config.js
     as extend.colors entries)
   - Confirmation that no gold (#C9A227) or old navy (#14356B) remains

Reminder: functionality, layouts, content, routes, and prototype links are
completely unchanged. This pass is colour, branding, and identity only.

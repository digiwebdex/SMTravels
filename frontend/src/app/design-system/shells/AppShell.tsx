/**
 * ERP app shell — thin re-export so consumers can import the ERP chrome from
 * the design system alongside PortalShell/MarketingShell.
 */
export { ErpLayout as AppShell, ErpLayout } from "../../erp/ErpLayout";
export type { ErpOutletCtx } from "../../erp/ErpLayout";

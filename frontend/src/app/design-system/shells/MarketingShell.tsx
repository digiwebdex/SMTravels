/**
 * Marketing (public website) shell — thin re-export so consumers can import
 * the public site chrome from the design system alongside PortalShell/AppShell.
 */
export { Layout as MarketingShell, Layout, Header, Footer } from "../../components/Layout";

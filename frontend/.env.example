# Copy to .env.production before building for production.
# STATIC build: this value is compiled into dist/ at build time (cannot change on the server).
VITE_API_URL=https://smtravels.digiwebdex.com/api

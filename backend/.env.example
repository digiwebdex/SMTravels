# ── SM Travels API — environment ─────────────────────────────────────────────
# Copy this file to `.env` for LOCAL development.
# On the server the real values live in /var/www/SMTravels/.env.production
# (loaded by the systemd unit smtravels-api.service).

NODE_ENV=development
# MUST stay 127.0.0.1 — the server firewall assumes localhost-only. NEVER 0.0.0.0.
HOST=127.0.0.1
PORT=4030

# ── Database (PostgreSQL) ────────────────────────────────────────────────────
# LOCAL dev: run YOUR OWN Postgres (see DEPLOYMENT.md "Local backend dev").
# Do NOT point this at the production database.
DATABASE_URL=postgresql://smtravels_user:devpassword@localhost:5432/smtravels_db?schema=public

# ── Auth (JWT) ───────────────────────────────────────────────────────────────
JWT_SECRET=dev-only-change-me
JWT_REFRESH_SECRET=dev-only-change-me-too

# ── CORS ─────────────────────────────────────────────────────────────────────
CORS_ORIGIN=https://smtravels.digiwebdex.com

# ── PII encryption (AES-256-GCM) ─────────────────────────────────────────────
# The app FAILS LOUDLY at startup if these are missing (it will never write
# plaintext into the "encrypted" passport/NID columns).
# Generate a key: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Rotation: add PII_KEK_2=<newkey>, bump PII_KEK_CURRENT_ID=2; a re-encrypt job
# migrates rows WHERE piiKeyId < current. Old rows still decrypt via their keyId.
PII_KEK_CURRENT_ID=1
PII_KEK_1=
PII_INDEX_KEY=

# ── Integrations (all optional — missing credentials fall back to log-only) ──

# SMTP (transactional email)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="SM Travels <noreply@smtravels.digiwebdex.com>"

# BulkSMSBD (SMS)
# BULKSMSBD_API_KEY=
# BULKSMSBD_SENDER_ID=

# Wasender (WhatsApp)
# WASENDER_API_URL=https://www.wasenderapi.com
# WASENDER_API_TOKEN=
# WASENDER_PHONE_NUMBER_ID=

# Google Gemini (AI assistant)
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash

# Google Cloud Vision OCR — prefer Service Account (ADC path to JSON key)
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/google-vision.json
# Legacy API-key auth (not used in production):
# GOOGLE_VISION_API_KEY=

# ── Payment gateways (DISABLED — manual NPSB bank transfer only) ─────────────
# bKash
# BKASH_BASE_URL=
# BKASH_APP_KEY=
# BKASH_APP_SECRET=
# BKASH_USERNAME=
# BKASH_PASSWORD=

# Nagad
# NAGAD_BASE_URL=
# NAGAD_MERCHANT_ID=
# NAGAD_MERCHANT_PRIVATE_KEY=
# NAGAD_PG_PUBLIC_KEY=

# SSLCommerz
# SSLCOMMERZ_STORE_ID=
# SSLCOMMERZ_STORE_PASSWORD=
# SSLCOMMERZ_IS_LIVE=false
